rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DEFAULT: Deny all public access.
      // The Python Admin SDK (used by the Hive backend) bypasses these rules
      // and has full read/write access automatically.
      allow read, write: if false;
    }
  }
}
